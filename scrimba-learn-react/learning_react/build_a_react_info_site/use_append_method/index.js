'use strict';

const navbar = (
  <nav>
    <h2>BrandName</h2>
    <ul>
      <li>About</li>
      <li>Menu</li>
      <li>Contact</li>
    </ul>
  </nav>
);

ReactDOM.createRoot(document.getElementById('root')).render(navbar);
